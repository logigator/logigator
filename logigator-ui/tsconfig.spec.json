/* Library unit-test config. Extends the workspace base tsconfig. */
{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "types": []
  },
  "include": ["src/**/*.spec.ts", "src/**/*.d.ts"]
}
