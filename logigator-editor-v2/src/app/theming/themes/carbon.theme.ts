import { Theme } from '../theme.model';

/** Cool graphite dark / cool blue-grey light. Overrides only the board color. */
export const CarbonTheme: Theme = {
  id: 'carbon',
  label: 'Carbon',
  dark: { background: 0x1a1f27 },
  light: { background: 0xe9eef6 }
};
