import { Theme } from '../theme.model';

/** Warm coffee dark / warm latte light. Overrides only the board color. */
export const MochaTheme: Theme = {
  id: 'mocha',
  label: 'Mocha',
  dark: { background: 0x241d16 },
  light: { background: 0xf3ece1 }
};
