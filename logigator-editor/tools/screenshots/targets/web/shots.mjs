/**
 * The features page's tour figures: one row per capability, the figure a
 * capture of the editor doing it.
 *
 * Empty until that page exists. The target is otherwise complete — its
 * languages, its two schemes, where the media lands and the import map over it
 * — so a run is refused with this file named rather than writing an empty
 * registry.
 *
 * A shot's `name` is its capability, and the file name adds the language and
 * scheme the pass is in, which is what `webTourMedia` reads back.
 */
export const SHOTS = [];
