import { ShortcutTextPipe } from './shortcut-text.pipe';

describe('ShortcutTextPipe', () => {
	it('create an instance', () => {
		const pipe = new ShortcutTextPipe();
		expect(pipe).toBeTruthy();
	});
});
