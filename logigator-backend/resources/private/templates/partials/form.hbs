<!-- parameters: action: string, method: string, class: string -->

<form class="partial-form {{class}}" action="{{action}}" method="{{ternary method method 'POST'}}" name="{{formName action}}">

	{{> @partial-block }}
</form>
