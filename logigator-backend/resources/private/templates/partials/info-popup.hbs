{{! parameters: name: string, title: string }}

{{#if (expr infoPopup.show '===' name) }}
	{{#infoPopupDataScope }}
		{{#> popup title=title defaultOpen=true }}
			{{> @partial-block }}
		{{/popup}}
	{{/infoPopupDataScope}}
{{/if}}
